#This file is generated by updateDaemonJvm
# WARNING (issue #105): do NOT re-run `updateDaemonJvm`. It regenerates the foojay download URLs
# and a `toolchainVendor` pin, which break IzzyOnDroid reproducible builds: their builders are
# network-restricted and provision their own JDK 21, so forcing a vendor-specific download fails.
# Keep ONLY the vendor-neutral version requirement, satisfied by any locally-installed JDK 21.
toolchainVersion=21
