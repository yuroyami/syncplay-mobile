#This file is generated by updateDaemonJvm
# Warning (#105): do not run `updateDaemonJvm` again. It writes foojay download URLs and a
# `toolchainVendor` pin, which break the IzzyOnDroid reproducible builds. Their builders have
# restricted network access and bring their own JDK 21, so a vendor download fails there.
# Keep only the version requirement below, which any installed JDK 21 meets.
toolchainVersion=21
