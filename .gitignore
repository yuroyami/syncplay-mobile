*.klib
.kotlin/
*.iml
.gradle
.idea
.DS_Store
build
captures
.externalNativeBuild
.cxx
local.properties
xcuserdata
*.apk
*.ap_
*.aab
*.dex
*.class
bin/
gen/
Thumbs.db
.classpath
.project
.idea/
.gradle/
build/
app/build/
AndroidAppOutput/
keystore/
obj/
*.jks
*.pem
*.so
libavcodec.so
libavdevice.so
libavfilter.so
libavformat.so
libavutil.so
libc++_shared.so
libmpv.so
libplayer.so
libpostproc.so
libswresample.so
lbswscale.so
Pods/
*.mychanges
*.kmpssot.bak

buildscripts/deps
buildscripts/deps/*
buildscripts/sdk
buildscripts/prefix
buildscripts/prefix64
*.h
*.tgz
syncplay-pc-src-master
# Hide python files which i offload into my project for quickly viewing syncplay official code
*.py

#we should also hide claude hidden dir which is used by Claude Code
.claude
# Consolidated engineering ledger, the single source of truth for open defects.
# Local only: it lists unfixed security findings, and this repo is public.
# Remove this line once the P0 security items in it are fixed, if you want it public.
SSOT.md

# Desktop: downloaded libVLC natives (fetched by :desktopApp:fetchVlcNatives, ~185MB)
desktopApp/resources/

# KiteSSOT plugin state: takeover-recovery backups and ownership manifests.
# Local machine state, regenerated by the kiteSsot* tasks; never code or assets.
.kitessot/
**/.kitessot/
*.kitessot.bak
