Audio files can be played, not only video, and they show a visualizer drawn by KitePlayer.

The keyboard no longer closes after the first letter on Home, so the name and room fields work again.

A remote reaches every control, so the app works on Android TV, and it lists the videos on the device.

Invites can be shared as an https link.

The room's side panels are now one panel. You can set the chat colours and reorder the playlist by dragging.

Track, seek and screen-on fixes.
